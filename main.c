#include <stdio.h>
#include <stdlib.h>

int soma(int a, int b);

int main()
{
    printf("Hello world!\n");
    return 0;
}

int soma(int a, int b)
{
    return (a+b);
}

int multiplicacao(int a, int b)
{
	return (a*b);
}
